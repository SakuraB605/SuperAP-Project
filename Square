package home.sj;

import java.awt.Color;
import java.util.ArrayList;

public class Square {
  private int num;
  private Color color;
  private ArrayList<Permutations> arr;
  private boolean isLocked;
/*Square(int number, Color color){
    num = number;
    this.color = color;
  }
*/
  Square(int number, ArrayList<Permutations> array, Color color){
    arr = new ArrayList<Permutations>();
    num = number;
    if (array != null) {      
      for (int i = 0; i < array.size(); i++) {
        arr.add(array.get(i));
      }
    }
    this.color = color;
  }
  Square(int number, ArrayList<Permutations> array, boolean isLocked, Color color){
	    arr = new ArrayList<Permutations>();
	    num = number;
	    if (array != null) {      
	      for (int i = 0; i < array.size(); i++) {
	        arr.add(array.get(i));
	      }
	    }
	    this.isLocked = isLocked;
	    this.color = color;
  }
  Square(boolean isLocked, Color color){
    this.isLocked = isLocked;
    this.color = color;
  }
  
  public boolean isLocked() {
	    return isLocked;
  }
  public int getNumber() {
    return num; 
  }
  public Color getColor() {
    return color; 
  }
  public ArrayList<Permutations> getArrayList() {
    return arr;
  }
  public boolean getIsLocked() {
    return isLocked;
  }
}
